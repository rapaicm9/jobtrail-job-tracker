<Project Sdk="Microsoft.NET.Sdk">
  <ItemGroup>
    <!-- The module defines its own minimal-API endpoints; the framework
         reference brings in ASP.NET Core without making this a host. -->
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Aspire.Npgsql.EntityFrameworkCore.PostgreSQL" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <!-- The integration fixture applies this module's migrations to the
         container database, and asserts the read model's shape against it. -->
    <InternalsVisibleTo Include="JobTrail.IntegrationTests" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\JobTrail.Modules.Analytics.Contracts\JobTrail.Modules.Analytics.Contracts.csproj" />
    <ProjectReference Include="..\..\..\JobTrail.SharedKernel\JobTrail.SharedKernel.csproj" />
    <ProjectReference Include="..\..\..\JobTrail.Infrastructure\JobTrail.Infrastructure.csproj" />
    <!-- The events the read model is built from. A Contracts reference only -
         this module never sees an application, just what was announced about it. -->
    <ProjectReference Include="..\..\Applications\JobTrail.Modules.Applications.Contracts\JobTrail.Modules.Applications.Contracts.csproj" />
    <!-- Consumes Identity's UserDataDeletionRequested to erase its own rows. -->
    <ProjectReference Include="..\..\Identity\JobTrail.Modules.Identity.Contracts\JobTrail.Modules.Identity.Contracts.csproj" />
  </ItemGroup>
</Project>
